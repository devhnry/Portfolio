import { Container } from "./Container.tsx";

export const Services = () => {
  return (
    <section className={`bg-black rounded-t-2xl relative z-30 h-screen`}>
      <Container classes={'py-7'}>
        <h2 className={`text-heading-2 uppercase font-inter heading-two font-medium`}>how i can help you/</h2>
      </Container>
    </section>
  );
};
